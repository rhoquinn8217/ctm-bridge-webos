TV/webOS repo. Canonical agent instructions live in `D:\Work\CMU\AGENTS.md` and `D:\Work\CMU\stupid.claude.md` — read them first.

The BINDING project philosophy lives in `D:\Work\CTM\ctm_bridge_requirements.md` (§3 Core
principles): every layer stays on its own — the server knows nothing, the service only interprets maps,
the maps describe what is happening. Valid for the bridge AND future streaming work.

Evaluate your performance so far. Try to detect if you are drifting from the scope.
